name: Azure Static Web Apps CI/CD

trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

jobs:
- job: build_and_deploy_job
  displayName: Build and Deploy Job
  pool:
    vmImage: ubuntu-latest
  variables:
    - group: Azure-Static-Web-Apps-nice-rock-0193cc903-variable-group

  steps:
    - checkout: self
      submodules: true

    - task: UsePythonVersion@0
      inputs:
        versionSpec: '3.11'
      displayName: 'Set up Python 3.11'

    - script: |
        pip install -r requirements.txt
        pip install jupyter pyyaml
      workingDirectory: presentations/digital_research_at_uon
      displayName: 'Install Python packages from course directory'

    - script: |
        curl -sSLO https://quarto.org/download/latest/quarto-linux-amd64.deb
        sudo dpkg -i quarto-linux-amd64.deb
        quarto --version
      displayName: 'Install Quarto CLI'

    - script: |
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends r-base
        R --version
      displayName: 'Install R'

    - script: |
        mkdir -p ~/.R/library
        echo 'R_LIBS_USER="~/.R/library"' >> ~/.Renviron
        Rscript -e 'install.packages("renv", repos = "https://cloud.r-project.org")'
      workingDirectory: presentations/digital_research_at_uon
      displayName: 'Install and configure renv'

    - script: |
        Rscript -e 'renv::restore()'
      workingDirectory: presentations/digital_research_at_uon
      displayName: 'Restore R environment with renv'

    - script: |
        Rscript -e 'renv::install(c("knitr", "rmarkdown")); renv::snapshot()'
      workingDirectory: presentations/digital_research_at_uon
      displayName: 'Install knitr and rmarkdown into renv'

    - script: |
        export R_LIBS_USER=$HOME/.R/library
        quarto render .
      workingDirectory: presentations/digital_research_at_uon
      displayName: 'Render Quarto site (with user R library)'

    - script: |
        test -f _site/index.html
      workingDirectory: presentations/digital_research_at_uon
      displayName: 'Verify site output'

    - task: AzureStaticWebApp@0
      inputs:
        azure_static_web_apps_api_token: $(AZURE_STATIC_WEB_APPS_API_TOKEN_NICE_ROCK_0193CC903)
        app_location: "presentations/digital_research_at_uon"
        output_location: "_site"
        api_location: ""
